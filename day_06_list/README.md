# Problem :  Create a shopping list as a list. Add, remove, and display items. Sort the list.

## Solution : 
- In main.py

## Notes : 
- The challenge is to make a list consisting of shopping items
- I used python lists to store the items
- Functions used :
  --
  - append() is used to add items in the list
  - remove() is used to remove items from the list
  - sort() is used to sort the list
- To display items we can use print function
- I used indexing and slicing to display the specific items from the list
