# Problem :  Use a tuple to store days of the week. Print “Weekend” for Saturday/Sunday using a loop.

## Solution : 
- In main.py file
## Notes : 
- Tuple is non-mutable data structure , which means that we cannot change the items inside a tuple
- In this problem, loop is also used. Loops help us to achieve an operation for given times
- Loops helps to make code more understandable
- Loops are used because sometimes we have to do same operations again and again
- Slicing technique is used to get the days required
