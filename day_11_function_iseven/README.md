# Problem : Define a function is_even(n) that returns True if the number is even. Test it
## Solution :
- In main.py
- Solution contains a function that takes a number and return true for even number and false otherwise.
## Notes : 
- A function is defined using the def keyword (used for user-defined functions).
- is_even(n) is the name of function and n is the placeholder variable for the number we want to test
- After defining the function , we have to call the function by using the name of function.
  
