# Problem : Create a greet_user(name) function with a default argument "Guest".
## Solution :
- In main.py
## Notes : 
- A function greet_user(name) is created and **name = "Guest"** is taken as defaul argument
- A variable created to get the input name from the user 
- Function is called with the name variable
