# Problem: Use a lambda function with map() to square a list of numbers.
## Solution: 
- In main.py
## Notes: 
- A lambda function is small, anonymous (nameless) fuction defined in a single line of code.
- Syntax of lambda function :--  ```lambda arguments: expression```
- In this challenge, map() function is also used. map() applies the lambda formula to every argument
  
