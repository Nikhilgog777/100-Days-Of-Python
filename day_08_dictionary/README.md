# Problem: Build a “dictionary phonebook”: store 3 contacts (name: number) and let the user look up a name.

## Solution : 
- In main.py
## Notes : 
- Dictionary is a type of data structure in python which consists of keys and values
- In this problem, contacts of three persons are stored using dictionary data structure
- .get() method is used to check the searched name by the user
- .get() prevents system to crash when the name is not found
- Finally, the name and number are printer using f strings
