# Problem : Write a factorial(n) function using recursion. Add a try/except for negative inputs.
## Solution:
- In mains.py
- Solution contains a recursive function with try and except to handle errors
## Notes:
- Recursive functions are the functions that call themselves over and over. They have a base condition to define the recursion
- In factorial function, base condition is that when n is 0 or 1 it returns 1 value
- In main.py, the function is defined as factorial(n) and in its definition it is called itself to find other values
- Error handling is also used in this problem using try and except statements
- try/except are used to display the value error whenever the value is negative 
