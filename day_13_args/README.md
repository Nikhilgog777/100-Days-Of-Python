# Problem : Use *args to write a sum_all() function that adds any number of arguments.
## Solution : 
- In main.py
## Notes : 
- *args is used to create a flexible function that can accept any number of positional arguments
- A function with *args can contain as many as argument we assign to it
- In this challenge, a function sum_all() is created which sums all the numbers assigned to it
