# Problem: Read a text file line by line and print only lines that contain the word “Python”
# Solution:
- In main.py
- Solution contains a python file which reads text file and prints the lines containing "Python"
# Notes:
- Used ' with open( . . . ) as ' in this problem because it is efficient than traditional open and close.
- for loop is used to traverse every line
- Conditional statement 'if' is used to check the condition required in the problem
- line.strip() is used remove whitespaces and newlines that are unnecessary.
### The text file used in this code is "Main.txt" given in the repository
