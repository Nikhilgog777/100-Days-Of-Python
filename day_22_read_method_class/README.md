# Problem: Add a read() method to Book that changes a is_read flag and prints a message.
## Solution:
- In main.py
## Notes:
- The concept of class and attributes used in this solution is in <a href="https://github.com/Nikhilgog777/100-Days-Of-Python/tree/main/day_21_oops_book_class" > `previous problem.` </a>
- ```is_read``` is defined `False` initially when the book is not read by the reader.
- After reading the book, ```read()``` function can be called on the book. After reading book, ```is_read``` will flag `True`.
- A message regarding the reading of book is also printed.
